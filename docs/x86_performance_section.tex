% ---------------------------------------------------------
% 4.1 测试环境设定
% ---------------------------------------------------------
\subsection{Evaluation Methodology and Platforms}
\label{sec:eval-methodology}

To ensure reproducibility and demonstrate the scheme's versatility across
deployment classes, we define the benchmarking environments and metrics as
follows.

\paragraph{Metric definition.}
On x86-64, performance is reported as \textbf{median CPU cycles} measured via
\texttt{RDTSC} with call overhead subtracted
(\texttt{cpucycles\_overhead()}); each entry is the median over five
independent runs of $N_{\mathrm{test}}{=}1{,}000$ timed iterations per run.
On embedded targets, performance is likewise reported as median CPU cycles
(median of $N_{\mathrm{test}}{=}10{,}000$ runs); we additionally report
maximum stack usage to assess suitability for memory-constrained devices.

\begin{itemize}
  \item \textbf{High-performance platform (x86\_64).}
        Single-socket KVM/QEMU guest reporting an \textbf{AMD EPYC 9655P}
        (96-core class, firmware clock \textbf{2.0\,GHz}, 2~vCPUs, 128\,KiB
        L1d/i per core, 1\,MiB L2 per core); host ISA features include AVX2,
        BMI2, POPCNT, and AVX-512.
        \begin{itemize}
          \item \textit{OS:} Linux \textbf{6.1.0-49-amd64} (Debian~12)
          \item \textit{Compiler:} GCC \textbf{12.2.0}
          \item \textit{Flags:} \texttt{-std=c99 -O3 -fomit-frame-pointer
                -march=native -mtune=native -mavx2 -mbmi2 -mpopcnt -flto}
          \item \textit{RNG (benchmark path):} Linux \texttt{getrandom(2)}
                (\texttt{USE\_GETRANDOM=1}), matching the ML-KEM reference;
                KAT paths retain the in-tree AES-256-CTR-DRBG.
        \end{itemize}
        All tables in \S\ref{sec:x86-perf} were collected on this platform.

  \item \textbf{Embedded platform (32-bit ARM).}
        STM32F407 Discovery Board (ARM Cortex-M4) @ 168\,MHz, 192\,KiB SRAM,
        1\,MiB Flash.
        \begin{itemize}
          \item \textit{Compiler:} \texttt{arm-none-eabi-gcc 14.3.rel1}
          \item \textit{Flags:} \texttt{-O3 -mthumb -mcpu=cortex-m4
                -mfloat-abi=hard -mfpu=fpv4-sp-d16}
        \end{itemize}

  \item \textbf{Embedded platform (32-bit RISC-V).}
        CM32M433R\_START Board (RV32IMACF) @ 144\,MHz, 144\,KiB SRAM,
        512\,KiB Flash.
        \begin{itemize}
          \item \textit{Compiler:} \texttt{riscv64-unknown-elf-gcc 14.2.1}
          \item \textit{Flags:} \texttt{-O3 -march=rv32imafc -mabi=ilp32f}
        \end{itemize}
\end{itemize}

% ---------------------------------------------------------
% 4.2 软件性能：x86 (Ref vs AVX2)
% ---------------------------------------------------------
\subsection{Software Performance on x86 Processors}
\label{sec:x86-perf}

Unless stated otherwise, the numbers below use the x86\_64 platform and cycle
methodology from \S\ref{sec:eval-methodology}.
We provide two ISO~C99 reference builds and one AVX2-optimized tree:
\begin{itemize}
  \item \textbf{NGCC configuration} (\texttt{Implementations/Reference\_Implementation/}):
        the ICCS submission path uses the SM3-family auxiliary functions in
        \texttt{auxfunc.c} (\texttt{sm3hash}, \texttt{pseudohash}, \texttt{pseudoXOF})
        wired through \texttt{symmetric.h}; matrix expansion uses the corresponding
        pseudo-XOF block size (168~B for \textsc{WEAVER-640}, 136~B otherwise).
        This configuration is used for NIST-style KAT generation and specification
        conformance.
  \item \textbf{Keccak reference configuration} (\texttt{ref/}): a portable Keccak/FIPS~202
        implementation (\texttt{symmetric-shake.c}) with SHA3-512 hashing (modes~3/5),
        SHAKE256 PRFs, and \textbf{SHAKE128} matrix XOFs at all security levels (matching
        ML-KEM's matrix-expansion primitive).
  \item \textbf{AVX2 implementation} (\texttt{avx2\_weaver/}): same Keccak symmetric layer
        as \texttt{ref/}; matrix expansion uses SHAKE128$\times$4 on all modes.
        AVX2 centered binomial sampling (\texttt{USE\_AVX\_CBD=1}) vectorizes
        $\eta\in\{2,3,7\}$ (Kyber-style paths for $\eta{=}2/3$; SWAR popcount for
        $\eta{=}7$); $\eta{=}9$ (\textsc{WEAVER-2048}) remains scalar-equivalent until a
        correct 18-bit cross-byte extract is in place.
        All performance numbers below are measured on this tree so that comparisons with
        ML-KEM (also Keccak-based, SHAKE128 matrix) isolate algorithmic differences rather
        than hash/XOF choice.
\end{itemize}

The AVX2 build vectorizes polynomial arithmetic, parallel matrix sampling, and
CBD at $\eta\le 7$.
For \textsc{WEAVER-640} ($(n,k,q){=}(128,5,3329)$) it enables an AVX2 NTT at
$n{=}128$, SHAKE128$\times$4 matrix generation, and fused 9-bit public-key
compression.
For \textsc{WEAVER-1024} ($(256,4,7681)$) and \textsc{WEAVER-2048}
($(512,4,7681)$) it adds a dedicated $q{=}7681$ AVX2 NTT, SHAKE128$\times$4
matrix generation, fused \texttt{polyvec\_basemul\_acc}, and AVX2 modular
reduction in standard coefficient order.
Portable \texttt{ref/} medians disable all \texttt{USE\_AVX\_*} optimization
switches (portable C NTT, scalar \texttt{gen\_matrix}, scalar compression).
ML-KEM baselines are from pq-crystals/kyber commit \texttt{4768bd3} (AVX2
build), collected on the same x86 platform with the same methodology.

\subsubsection{Weaver: Reference vs.\ AVX2}

Table~\ref{tab:x86_perf} reports the self-comparison on the current Table~2
parameter sets (Keccak reference configuration throughout; SHAKE128 matrix XOF).
AVX2 speedups range from \textbf{2.0$\times$--3.1$\times$}, with the largest
absolute gains on $q{=}7681$ NTTs and parallel \texttt{gen\_matrix}; fused base
multiplication at $q{=}3329$, $n{=}128$ is already near scalar cost and therefore
contributes less to the overall ratio.
Unifying modes~3/5 on SHAKE128 (168-byte blocks vs.\ the previous 136-byte
SHAKE256 matrix path) reduces \texttt{gen\_matrix} on \textsc{WEAVER-1024} by
$\approx{}21\%$ and on \textsc{WEAVER-2048} by $\approx{}20\%$ relative to the
prior AVX2 build.

\begin{table}[t]
\centering
\caption{Weaver KEM performance: portable \texttt{ref/} vs.\ AVX2
        (median CPU cycles, Keccak/SHAKE128-matrix config).}
\label{tab:x86_perf}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llrrr}
\toprule
\textbf{Scheme} & \textbf{Impl.} & \textbf{KeyGen} & \textbf{Encaps} & \textbf{Decaps} \\
\midrule
\multirow{2}{*}{\shortstack[l]{\textsc{WEAVER-640}\\$(128,5,3329)$}}
  & Ref (C99)  & 111{,}773 & 119{,}001 & 132{,}105 \\
  & AVX2       & \textbf{50{,}361}  & \textbf{55{,}457}  & \textbf{51{,}453}  \\
\midrule
\multirow{2}{*}{\shortstack[l]{\textsc{WEAVER-1024}\\$(256,4,7681)$}}
  & Ref (C99)  & 198{,}977 & 241{,}305 & 260{,}441 \\
  & AVX2       & \textbf{98{,}695}  & \textbf{108{,}133} & \textbf{85{,}305}  \\
\midrule
\multirow{2}{*}{\shortstack[l]{\textsc{WEAVER-2048}\\$(512,4,7681)$}}
  & Ref (C99)  & 413{,}919 & 552{,}889 & 587{,}287 \\
  & AVX2       & \textbf{196{,}637} & \textbf{266{,}785} & \textbf{215{,}721} \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Comparison with ML-KEM}

Tables~\ref{tab:kem_avx}--\ref{tab:overhead_breakdown} compare Weaver against
ML-KEM at the closest security level (128/256/512-bit classical targets).
Because \textsc{WEAVER-640} uses $(n,k){=}(128,5)$ rather than ML-KEM-512's
$(256,2)$, the 128-bit row is indicative rather than a matched-dimension
comparison.
At 256-bit security, \textsc{WEAVER-1024} matches ML-KEM-1024 on $(n,k)$ but
employs $q{=}7681$, $\eta_1{=}7$, and tighter compression; it is
\textbf{4.2$\times$} / \textbf{4.4$\times$} / \textbf{3.2$\times$} slower in
KeyGen / Encaps / Decaps despite comparable \texttt{polyvec\_basemul\_acc} latency.
In exchange, \textsc{WEAVER-1024} reduces $|\mathsf{pk}|$ by 16\% and total
wire size by 9\% (Table~\ref{tab:bandwidth}); \textsc{WEAVER-640} matches
ML-KEM-512 on total on-the-wire bytes (1{,}568~B).

\begin{table}[t]
\centering
\caption{End-to-end KEM performance vs.\ ML-KEM (median CPU cycles, AVX2/Keccak).}
\label{tab:kem_avx}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llcrrr}
\toprule
\textbf{Scheme} & $(n,k,q)$ & \textbf{Sec.} & \textbf{KeyGen} & \textbf{Encaps} & \textbf{Decaps} \\
\midrule
ML-KEM-512   & $(256,2,3329)$   & 128-bit & 10{,}840 & 11{,}595 & 12{,}690 \\
\textsc{WEAVER-640} & $(128,5,3329)$   & 128-bit & 50{,}361 & 55{,}457 & 51{,}453 \\
\midrule
ML-KEM-768   & $(256,3,3329)$   & 192-bit & 18{,}330 & 18{,}280 & 19{,}915 \\
ML-KEM-1024  & $(256,4,3329)$   & 256-bit & 23{,}555 & 24{,}390 & 27{,}120 \\
\textsc{WEAVER-1024} & $(256,4,7681)$   & 256-bit & 98{,}695 & 108{,}133 & 85{,}305 \\
\midrule
\textsc{WEAVER-2048} & $(512,4,7681)$   & 512-bit & 196{,}637 & 266{,}785 & 215{,}721 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Ciphertext and public-key sizes (bytes).}
\label{tab:bandwidth}
\begin{tabular}{lrrr}
\toprule
\textbf{Scheme} & $|\mathsf{pk}|$ & $|\mathsf{ct}|$ & Total \\
\midrule
ML-KEM-512   & 800   & 768   & 1{,}568 \\
\textsc{WEAVER-640}   & 752   & 816   & \textbf{1{,}568} \\
ML-KEM-768   & 1{,}184 & 1{,}088 & 2{,}272 \\
ML-KEM-1024  & 1{,}568 & 1{,}568 & 3{,}136 \\
\textsc{WEAVER-1024}  & 1{,}312 & 1{,}536 & \textbf{2{,}848} \\
\textsc{WEAVER-2048}  & 2{,}880 & 3{,}392 & 6{,}272 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Overhead Decomposition}

Table~\ref{tab:overhead_breakdown} attributes the encapsulation gap on the
matched 256-bit row to three categories (component medians from the AVX2
Keccak/SHAKE128-matrix build; ML-KEM rows from the same harness).
%
\emph{Design-intrinsic} components---randomized lifting $\mathrm{Inv}_q$ and BCH
message encoding (\texttt{poly\_frommsg})---are absent from ML-KEM; together
they account for $+5{,}430$ cycles at $(128,5)$ and $+5{,}480$ cycles at
$(256,4)$ (segment medians from \texttt{bin/profile\_encap\_*});
i.e.\ about 14\% and 7\% of the respective encapsulation gaps vs.\ ML-KEM.
%
\emph{Parameter-induced} overhead is dominated on \textsc{WEAVER-1024} by
$\eta_1{=}7$ CBD ($+19{,}200$ cycles vs.\ ML-KEM-1024's $\eta_1{=}2$ sampling)
and by the $q{=}7681$ NTT ($\approx{}1{,}800$ cycles per polynomial vs.\
$\approx{}100$ cycles at $q{=}3329$).
At $(128,5)$ both ML-KEM-512 and \textsc{WEAVER-640} use $\eta_1{=}3$; CBD
adds $+1{,}850$ cycles in the encaps path despite the larger module rank
($k{=}5$).
%
\emph{Implementation-aligned} \texttt{gen\_matrix} is now within
\textbf{1.2$\times$} of ML-KEM's \texttt{gen\_a} at $(256,4)$ after unifying
on SHAKE128 ($\approx{}9.7$\,k vs.\ $\approx{}8.1$\,k cycles); at $(128,5)$ the
higher module rank still drives a $\approx{}12.6$\,k vs.\ $\approx{}2.0$\,k
cycle gap.

The same split explains key generation: $\mathrm{Inv}_q$ and BCH are not
invoked, yet \textsc{WEAVER-1024} key generation exceeds ML-KEM-1024 by
$\approx{}75{,}000$ cycles because of four $q{=}7681$ NTTs during public-key
packing/unpacking and four \texttt{gen\_matrix} calls per keypair.
On \textsc{WEAVER-640}, 9-bit compressed public-key packing
($\approx{}1{,}600$ cycles per \texttt{polyvec\_compress\_pk}) and five
secret/noise polynomials account for most of the remaining KeyGen gap.
Note that \texttt{kem\_decaps} is faster than \texttt{kem\_encaps} on
\textsc{WEAVER-1024} and \textsc{WEAVER-2048} because the FO re-encryption
inside decapsulation omits the \texttt{randombytes} draw present in
\texttt{kem\_encaps}.

\begin{table}[h]
\centering
\caption{Encapsulation overhead breakdown: Weaver vs.\ ML-KEM (AVX2/Keccak,
         median cycles). Components marked~$\dagger$ are absent in ML-KEM.
         The $(128,5)$ column compares \textsc{WEAVER-640} against ML-KEM-512
         at the same 128-bit target; dimensions differ.}
\label{tab:overhead_breakdown}
\renewcommand{\arraystretch}{1.12}
\small
\begin{tabular}{lrrrrrr}
\toprule
 & \multicolumn{3}{c}{\textbf{$(128,5)$ vs.\ ML-KEM-512}} &
   \multicolumn{3}{c}{\textbf{$(256,4)$ vs.\ ML-KEM-1024}} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
\textbf{Component} & ML-KEM & Weaver & $\Delta$ & ML-KEM & Weaver & $\Delta$ \\
\midrule
\texttt{gen\_matrix}              & 2{,}030 & 12{,}635 & +10{,}605
                                  & 8{,}060 & 9{,}749  & +1{,}689 \\
\texttt{poly\_getnoise} (CBD)     & 3{,}170 & 5{,}043  & +1{,}873
                                  & 1{,}970 & 21{,}241 & +19{,}271 \\
NTT + \texttt{basemul}            & 520     & 3{,}352  & +2{,}832
                                  & 520     & 7{,}902  & +7{,}382 \\
\textbf{Inv}$_q$ ($\dagger$)      & ---     & \textbf{5{,}069}  & \textbf{+5{,}069}$^\dagger$
                                  & ---      & \textbf{4{,}809} & \textbf{+4{,}809}$^\dagger$ \\
\textbf{BCH MsgEncode} ($\dagger$)& 25      & \textbf{363}  & \textbf{+338}$^\dagger$
                                  & 25       & \textbf{675}  & \textbf{+650}$^\dagger$ \\
Compress + pack                   & 500     & 1{,}610  & +1{,}110
                                  & 500      & 2{,}260  & +1{,}760 \\
\texttt{randombytes} + hash       & 3{,}350 & 3{,}900  & +550
                                  & 5{,}500  & 5{,}800  & +300 \\
\midrule
\textbf{Total (KEM Encaps)}       & \textbf{11{,}595} & \textbf{55{,}457} & \textbf{+43{,}862}
                                  & \textbf{24{,}390} & \textbf{108{,}133} & \textbf{+83{,}743} \\
\bottomrule
\end{tabular}
\smallskip\\
{\footnotesize $^\dagger$\,Direct cost of randomized lifting $\mathrm{Inv}_q$
(tight MLWR reduction) and BCH coding (aggressive compression).
Weaver $\mathrm{Inv}_q$/BCH/\texttt{gen\_matrix}/CBD/compress rows are segment
medians from \texttt{bin/profile\_encap\_512} and
\texttt{bin/profile\_encap\_1024} (\texttt{tools/profile\_encap\_decaps.c});
ML-KEM rows from the Kyber AVX2 harness.}
\end{table}

Excluding the two design-intrinsic components ($\mathrm{Inv}_q$ and BCH), the
remaining encapsulation overhead is $+38{,}247$ and $+77{,}608$ cycles on
\textsc{WEAVER-640} (128-bit row) and \textsc{WEAVER-1024} (256-bit row),
respectively.
On the matched $(256,4)$ row this corresponds to $93\%$ of the measured excess;
CBD, $q{=}7681$ NTT, and compression dominate, while $\mathrm{Inv}_q$
and BCH together contribute under 7\%.
The performance gap is therefore a combination of deliberate bandwidth--security
trade-offs \emph{and} the heavier $q{=}7681$ / $\eta_1{=}7$ parameterization,
not a shortfall in \texttt{polyvec\_basemul\_acc} (which matches ML-KEM-1024
at $\approx{}155$ cycles on both sides).

Component-level timings on the AVX2 Keccak build show that
\texttt{gen\_matrix} at $(256,4)$ is $\approx{}1.2\times$ ML-KEM's
\texttt{gen\_a} ($\approx{}9.7$\,k vs.\ $\approx{}8.1$\,k cycles) after
unifying matrix expansion on SHAKE128; Keccak $\times$4 absorption still
accounts for the majority of that kernel.
Single-polynomial NTT at $q{=}7681$ costs $\approx{}1{,}800$ cycles vs.\
$\approx{}100$ cycles for ML-KEM's $q{=}3329$ AVX2 NTT.
The \textsc{WEAVER-640} KeyGen gap vs.\ ML-KEM-512 is driven primarily by
$k{=}5$ matrix expansion ($\approx{}12.6$\,k cycles per \texttt{gen\_matrix})
and 9-bit public-key packing ($\approx{}1.6$\,k cycles per
\texttt{polyvec\_compress\_pk}).

On \textsc{WEAVER-2048}, end-to-end performance is dominated by matrix sampling
and serialization; \texttt{gen\_matrix} costs $\approx{}17.4$\,k cycles per call
in the AVX2 build, and fused \texttt{polyvec\_basemul\_acc} accounts for
$\approx{}3.3$\,k cycles per dot product at $n{=}512$.
